apiVersion: apps/v1
kind: Deployment
metadata:
  name: securebooks
spec:
  replicas: 2
  template:
    spec:
      containers:
        - name: securebooks
          env:
            - name: ENVIRONMENT
              value: dev
            - name: LOG_LEVEL
              value: DEBUG
